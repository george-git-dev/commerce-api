spring.application.name=commerce-api

# Profile padrao local: H2, sem precisar de Postgres rodando na maquina.
# Em producao, ativar via variavel de ambiente: SPRING_PROFILES_ACTIVE=postgres
spring.profiles.active=h2

spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
